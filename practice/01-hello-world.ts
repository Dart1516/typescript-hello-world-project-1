// Example 1: Store and print a simple message.
const firstMessage: string = "Hello, World!";
console.log(firstMessage);

// Example 2: Build a message with a name.
const studentName: string = "David";
const welcomeMessage: string = `Hello, ${studentName}!`;
console.log(welcomeMessage);
