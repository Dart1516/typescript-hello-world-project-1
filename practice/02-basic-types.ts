// Example 1: Use strings, numbers, and booleans.
const bookTitle: string = "TypeScript Basics";
const bookPages: number = 120;
const bookIsOpen: boolean = true;
console.log(bookTitle, bookPages, bookIsOpen);

// Example 2: Change a number and print its type.
let score: number = 10;
score = score + 5;
const scoreType: string = typeof score;
console.log(score, scoreType);
